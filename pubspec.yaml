name: highfield_zone_league
description: Highfields Zone League App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  supabase_flutter: ^2.8.2
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  animate_do: ^4.1.1
  image_picker: ^1.1.2

flutter:
  uses-material-design: true
dependency_overrides:
  image_picker_android: ^0.8.13+2
  flutter_plugin_android_lifecycle: ^2.0.32